// Package handlers provides HTTP handlers for the Probelet API.
//
// Each handler adapts HTTP requests to the shared application logic and writes
// structured JSON responses back to the client.
package handlers
